% interpreter is a program that evaluates a program

h(X, Y) :- f(X), g(Y).

% the level of program being interpreted is object-level
% the level of meta-interpreter is the meta-level
% meta-interpreter call itself -> meta-circular

% reflection and introspection: a program ability to examine itself